[project]
name = "ofms-reconstruction-qa-tests"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
    "robotframework>=7.4.1",
]

[tool.uv]
index-strategy = "unsafe-best-match"
python-downloads = "never"
